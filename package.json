{
  "name": "restaurant-la-bonne-etape",
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/ && cp style.css dist/ && cp robots.txt dist/ && cp sitemap.xml dist/ && cp og-image.svg dist/ && cp .assetsignore dist/ && cp dist_worker.js dist/_worker.js",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.79.0"
  }
}

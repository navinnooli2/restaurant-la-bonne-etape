{
  "name": "restaurant-la-bonne-etape",
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp index.html style.css robots.txt sitemap.xml og-image.svg favicon.svg .assetsignore dist/ && cp dist_worker.js dist/_worker.js && cp -r images dist/",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.79.0"
  }
}
